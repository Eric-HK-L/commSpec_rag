{
  "name": "commspec-rag-hooks",
  "private": true,
  "description": "Git hooks tooling for the repo root (husky + lint-staged + prettier)",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  }
}
